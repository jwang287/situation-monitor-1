# 通用组件测试用例

## 1. Badge 组件

**测试场景 1: 基本渲染**
- **操作**: 渲染 Badge 组件，传入 text 属性
- **预期**: 
  - 组件正确渲染
  - 显示传入的文本
  - 应用默认样式

**测试场景 2: 不同类型**
- **操作**: 渲染 Badge 组件，传入不同的 type 属性（success, warning, error, info）
- **预期**: 
  - 组件根据 type 应用不同的样式
  - 颜色符合预期

**测试场景 3: 自定义类名**
- **操作**: 渲染 Badge 组件，传入 class 属性
- **预期**: 
  - 自定义类名被应用
  - 与默认样式共存

**测试场景 4: 空文本**
- **操作**: 渲染 Badge 组件，不传入 text 属性
- **预期**: 
  - 组件仍能正常渲染
  - 不显示文本

## 2. BilingualText 组件

**测试场景 1: 基本渲染**
- **操作**: 渲染 BilingualText 组件，传入 zh 和 en 属性
- **预期**: 
  - 组件正确渲染
  - 显示中文和英文文本
  - 布局合理

**测试场景 2: 只传入中文**
- **操作**: 渲染 BilingualText 组件，只传入 zh 属性
- **预期**: 
  - 组件正确渲染
  - 只显示中文文本

**测试场景 3: 只传入英文**
- **操作**: 渲染 BilingualText 组件，只传入 en 属性
- **预期**: 
  - 组件正确渲染
  - 只显示英文文本

**测试场景 4: 自定义类名**
- **操作**: 渲染 BilingualText 组件，传入 class 属性
- **预期**: 
  - 自定义类名被应用
  - 样式正确

## 3. ErrorMessage 组件

**测试场景 1: 基本渲染**
- **操作**: 渲染 ErrorMessage 组件，传入 message 属性
- **预期**: 
  - 组件正确渲染
  - 显示错误消息
  - 应用错误样式

**测试场景 2: 空消息**
- **操作**: 渲染 ErrorMessage 组件，不传入 message 属性
- **预期**: 
  - 组件不显示
  - 不占用布局空间

**测试场景 3: 自定义类名**
- **操作**: 渲染 ErrorMessage 组件，传入 class 属性
- **预期**: 
  - 自定义类名被应用
  - 与默认样式共存

**测试场景 4: 带图标**
- **操作**: 渲染 ErrorMessage 组件，传入 showIcon 属性为 true
- **预期**: 
  - 显示错误图标
  - 图标与文本布局合理

## 4. HeatmapCell 组件

**测试场景 1: 基本渲染**
- **操作**: 渲染 HeatmapCell 组件，传入 value 属性
- **预期**: 
  - 组件正确渲染
  - 根据 value 应用相应的颜色
  - 显示 value 文本

**测试场景 2: 不同值范围**
- **操作**: 渲染 HeatmapCell 组件，传入不同的 value 值（-1 到 1 之间）
- **预期**: 
  - 颜色根据值的大小变化
  - 负值显示冷色调，正值显示暖色调

**测试场景 3: 自定义类名**
- **操作**: 渲染 HeatmapCell 组件，传入 class 属性
- **预期**: 
  - 自定义类名被应用
  - 与默认样式共存

**测试场景 4: 无值**
- **操作**: 渲染 HeatmapCell 组件，不传入 value 属性
- **预期**: 
  - 组件正确渲染
  - 显示默认样式
  - 不显示值

## 5. LoadingSpinner 组件

**测试场景 1: 基本渲染**
- **操作**: 渲染 LoadingSpinner 组件
- **预期**: 
  - 组件正确渲染
  - 显示旋转动画
  - 应用默认样式

**测试场景 2: 不同大小**
- **操作**: 渲染 LoadingSpinner 组件，传入 size 属性（sm, md, lg）
- **预期**: 
  - 组件根据 size 调整大小
  - 动画保持流畅

**测试场景 3: 不同颜色**
- **操作**: 渲染 LoadingSpinner 组件，传入 color 属性
- **预期**: 
  - 组件使用指定的颜色
  - 动画效果正常

**测试场景 4: 自定义类名**
- **操作**: 渲染 LoadingSpinner 组件，传入 class 属性
- **预期**: 
  - 自定义类名被应用
  - 与默认样式共存

## 6. MarketItem 组件

**测试场景 1: 基本渲染**
- **操作**: 渲染 MarketItem 组件，传入必要的属性（symbol, name, price, changePercent）
- **预期**: 
  - 组件正确渲染
  - 显示股票代码、名称、价格和涨跌幅
  - 涨跌幅颜色根据值的正负变化

**测试场景 2: 上涨状态**
- **操作**: 渲染 MarketItem 组件，传入 positive changePercent
- **预期**: 
  - 涨跌幅显示为绿色
  - 显示上涨箭头

**测试场景 3: 下跌状态**
- **操作**: 渲染 MarketItem 组件，传入 negative changePercent
- **预期**: 
  - 涨跌幅显示为红色
  - 显示下跌箭头

**测试场景 4: 零变化**
- **操作**: 渲染 MarketItem 组件，传入 changePercent 为 0
- **预期**: 
  - 涨跌幅显示为默认颜色
  - 不显示箭头

**测试场景 5: 可选属性**
- **操作**: 渲染 MarketItem 组件，传入 volume 属性
- **预期**: 
  - 显示成交量信息
  - 布局合理

## 7. NewsItem 组件

**测试场景 1: 基本渲染**
- **操作**: 渲染 NewsItem 组件，传入必要的属性（title, source, link, timestamp）
- **预期**: 
  - 组件正确渲染
  - 显示新闻标题、来源和时间
  - 链接正确设置

**测试场景 2: 带警报标记**
- **操作**: 渲染 NewsItem 组件，传入 isAlert 为 true
- **预期**: 
  - 显示警报标记
  - 应用警报样式

**测试场景 3: 带描述**
- **操作**: 渲染 NewsItem 组件，传入 description 属性
- **预期**: 
  - 显示新闻描述
  - 布局合理

**测试场景 4: 不同时间格式**
- **操作**: 渲染 NewsItem 组件，传入不同格式的 timestamp
- **预期**: 
  - 时间显示正确格式化
  - 相对时间显示合理

## 8. Panel 组件

**测试场景 1: 基本渲染**
- **操作**: 渲染 Panel 组件，传入 title 属性和内容
- **预期**: 
  - 组件正确渲染
  - 显示标题
  - 内容区域显示子组件

**测试场景 2: 带加载状态**
- **操作**: 渲染 Panel 组件，传入 loading 为 true
- **预期**: 
  - 显示加载动画
  - 内容区域被加载状态覆盖

**测试场景 3: 带错误状态**
- **操作**: 渲染 Panel 组件，传入 error 属性
- **预期**: 
  - 显示错误消息
  - 内容区域被错误状态覆盖

**测试场景 4: 带图标**
- **操作**: 渲染 Panel 组件，传入 icon 属性
- **预期**: 
  - 显示指定的图标
  - 图标与标题布局合理

**测试场景 5: 自定义类名**
- **操作**: 渲染 Panel 组件，传入 class 属性
- **预期**: 
  - 自定义类名被应用
  - 与默认样式共存

## 9. SkeletonPanel 组件

**测试场景 1: 基本渲染**
- **操作**: 渲染 SkeletonPanel 组件
- **预期**: 
  - 组件正确渲染
  - 显示骨架屏动画
  - 布局与 Panel 组件一致

**测试场景 2: 不同大小**
- **操作**: 渲染 SkeletonPanel 组件，传入 size 属性
- **预期**: 
  - 组件根据 size 调整大小
  - 骨架屏动画保持流畅

**测试场景 3: 自定义类名**
- **操作**: 渲染 SkeletonPanel 组件，传入 class 属性
- **预期**: 
  - 自定义类名被应用
  - 与默认样式共存

## 10. VersionBadge 组件

**测试场景 1: 基本渲染**
- **操作**: 渲染 VersionBadge 组件
- **预期**: 
  - 组件正确渲染
  - 显示版本号
  - 应用默认样式

**测试场景 2: 自定义版本号**
- **操作**: 渲染 VersionBadge 组件，传入 version 属性
- **预期**: 
  - 显示指定的版本号
  - 样式正确

**测试场景 3: 点击事件**
- **操作**: 渲染 VersionBadge 组件，添加点击事件监听器
- **预期**: 
  - 点击时触发事件
  - 事件处理函数被调用
